apiVersion: 1
datasources:
    - name: Events Service
      type: prometheus
      # Access mode - proxy (server in the UI) or direct (browser in the UI).
      access: proxy
      url: http://prometheus:9090
      scrape_interval: 1s
      editable: true
      query_timeout: 4s

